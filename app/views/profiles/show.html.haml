-# TODO make get rid of bootstrap stylings and use bourbon
.span4=image_tag @profile.user.gravatar_url, class: 'center-block'
.span5
  .panel.panel-success.well
    .panel-heading
      %h4="#{@profile.user.first_name} #{@profile.user.last_name}"
      %hr
    .panel-body
      %h5= @profile.user.email
      %h5=link_to @profile.user.blog, @profile.user.blog_url
      %h5
        = link_to "Github Repo", "http://github.com/#{@profile.user.gh_name}"

%table.table.table-striped
  %thead
    %tr
      %th
      %th
      %th
      %th
  %tbody
    -@assignments.sort_by{|a| a.date}.each do |assignment|
      %tr
        %td
          =link_to assignment.date.strftime("%m/%d"), assignment_path(assignment)
        %td
          - if @profile.user.homework_for(assignment) != nil
            - if @profile.user.homework_for(assignment).finished?
              %span.label.label-success
                = link_to "Finished", @profile.user.homework_for(assignment).url
            -else
              %span.label.label-warning
                = link_to "Incomplete", @profile.user.homework_for(assignment).url
        -if @profile.user == current_user && !@profile.user.homework_for(assignment).blank?
          %td
            =link_to "Description", homework_path(@profile.user.homework_for(assignment).id)
          %td
            =link_to "Edit", edit_homework_path(@profile.user.homework_for(assignment))
        -else
          %td
          %td
